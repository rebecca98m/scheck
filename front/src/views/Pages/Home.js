
import {startLoad} from "../../utils/utils";
const Home = () => {
    return (
        <div>
            Benvenuto

            <button onClick={startLoad}>Premi</button>
        </div>
    )
};

export default Home;